prompt: uhm when trying to update the activation time it doesnt allows to change the text to later update. seems to automatically refill itwith the backend text or idk yet, could you check?

answer: Fixed the Programs panel draft synchronization so persisted profile refreshes no longer overwrite dirty local edits, and changed activation time editing to a text-style HH:mm field.

suggestion: Reload the app before testing the Programs UI fix.
