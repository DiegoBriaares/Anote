Publish only the intended changes to the repository's configured `origin` as a new branch and draft pull request targeting `main`. Use a precise title and description based on the actual diff, tests, API/data considerations, and deployment impact. Never stage unrelated worktree changes or files containing credentials, personal paths, machine names, tailnet identifiers, databases, uploads, or other private data.
