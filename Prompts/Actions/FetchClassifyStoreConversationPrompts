Review the prompts from the current conversation and group them by the bug or enhancement they address.

For each group, create a descriptive slug directory under `Prompts/Bugs/` or `Prompts/Enhancements/` and store a descriptive Markdown file there. Preserve the request's intent and order, but sanitize every record before writing: replace personal filesystem paths, usernames, machine names, hostnames, tailnet domains, IP addresses, credentials, tokens, database contents, uploaded data, and other private values with neutral placeholders. Never archive secrets verbatim.

Use this structure for each entry, with blank lines between fields and `---` between entries:

`prompt: <sanitized prompt>`

`answer: <brief description of the result>`

`suggestion: <brief next step, or "None">`

Create the corresponding natural-language issue record in `Bugs/Solved/` or `Enhancements/Solved/`. Name it `NNN - <slug>`, using the next three-digit number across the relevant directory and its `Solved/` descendants.
