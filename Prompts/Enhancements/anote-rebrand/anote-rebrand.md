prompt: Sorry I interrupted to clarify that current changes in repo where the app name is changed to anote should persist here, and actually please do that changes here as well :)

answer: Continued the global prompt workflow with Anote as the app name scope, preserving the existing repo rename direction and preparing local user-facing defaults for the same brand.

sugestion: Keep the Anote rename grouped with the completion release so deployment updates the active app name and persistence fix together.

---

prompt: I mean continue running <repository-root>/Prompts/Actions/CompiledPrompts/FetchAndStorePromptsAndGlobalPushAndDeploy/BasePrompts and do the changes related to the app name :)

answer: Updated live app defaults, HTML metadata, package naming, README setup labels, production seed defaults, and compiled action prompt paths to use Anote and the current repository path.

sugestion: Restart the API after deploy so unchanged legacy app_config defaults migrate to Anote.
