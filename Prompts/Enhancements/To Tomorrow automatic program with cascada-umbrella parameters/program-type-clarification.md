prompt: It comes with an add program choice, Good with respect to crud but for now its only to tomorrow programs right?

answer: Confirmed that each row currently executes the same To Tomorrow Program behavior even though the CRUD table is structured for future program types.

suggestion: Introduce an explicit program type field only when a second behavior is implemented.
