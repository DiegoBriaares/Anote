# Anote Rebrand

Renamed the active application defaults to Anote across package metadata, the browser title, runtime fallback labels, backend default configuration, production seed defaults, README setup labels, and the compiled global push/deploy prompt paths. The backend now migrates unchanged legacy default app configuration values to Anote while leaving user-customized configuration intact.

Validation:
- `npm run build`
