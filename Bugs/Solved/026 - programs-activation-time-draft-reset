The Programs dashboard previously mirrored persisted profile program data into local form drafts whenever the store refreshed. If a profile refresh occurred while editing the activation time, the input could appear to refill with the saved value before the user could finish the update. The panel now preserves dirty local drafts during store refreshes and uses a text-style HH:mm input so partial edits remain controllable until Save Programs is pressed.
