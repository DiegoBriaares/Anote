Manual logout could render a blank patterned screen instead of the login page because the Logout button passed the React click event directly into the logout function. The store treated that event object as the login error message, which broke the login screen render after the user state was cleared. The fix makes the button call logout without arguments and only preserves logout messages when they are explicit strings.
