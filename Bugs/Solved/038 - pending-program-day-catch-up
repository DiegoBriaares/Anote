The To Tomorrow automatic program could miss inactive days when the app was not opened across one or more activation windows. The fix adds per-program pending day tracking, seeds the current day during active use, combines stored pending days with reconstructed missed activation days, moves stale source days into the current day, and only advances scheduler clock state after the full automatic batch succeeds.
