Rename the active application brand to Anote. The change should update user-facing defaults, browser metadata, package metadata, setup documentation, production seed defaults, and active compiled action prompt paths while preserving historical archives. Existing database app configuration values that still match legacy defaults should migrate safely to Anote without overwriting customized app titles or subtitles.
